# ── Core ──────────────────────────────────────────────────────────────────────
# Python >= 3.11 requerido

# Banco de dados
sqlalchemy[asyncio]>=2.0.39,<3
asyncpg>=0.30.0              # driver async para PostgreSQL
psycopg[binary]>=3.2.9,<4   # driver sync (usado por alembic)
advanced-alchemy>=1.0.1,<2  # UUIDAuditBase + utilitários SQLAlchemy

# Fila de tarefas (substitui litestar-saq no contexto standalone)
saq>=0.12.0                  # SAQ worker e CronJob
redis>=5.0.0                 # cliente Redis (asyncio)

# LangChain / Azure OpenAI
langchain-core>=0.3.46,<0.4
langchain-openai>=0.3.9,<0.4

# Pydantic
pydantic>=2.10.6,<3

# HTTP
requests>=2.32.0

# Criptografia / JWT (auth.py — tokens Azure)
cryptography>=44.0.0
PyJWT>=2.9.0

# Timezone
pytz>=2024.1

# HTML summary
pandas>=2.3.1

# Variáveis de ambiente (.env)
python-dotenv>=1.0.0
